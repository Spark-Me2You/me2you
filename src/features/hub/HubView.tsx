/**
 * Hub View — "Network" screen
 * Now displays a Pixi.js animation with walking characters
 */

import React from 'react';
import { PixiHub } from './components/PixiHub';

export const HubView: React.FC = () => {
  return <PixiHub />;
};
